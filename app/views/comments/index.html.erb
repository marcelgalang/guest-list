<h2>Event: <%=@list.name %></h2>

<div class="comments">
  <%= render 'comments/comments' %>
</div>

<%= form_for ([@list, @comment])do |f|  %>
  <%= f.text_field :content %>
  <!-- <%# @comment.user = :current_user %> -->
  <!-- <p><%= f.submit  :data => { disable_with: false }%></p> -->
  <p><%= f.submit "Add Comment" , :data => { disable_with: false }%></p>
<% end %>

<a href='/lists/<%=@list.id%>/guests'>View Event Guests</a>
