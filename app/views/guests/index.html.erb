<h2>Event: <%=@list.name %></h2>

<div class="guests" >
  <ol>
    <% @guests.each do |guest| %>
      <li><%= guest.name %></li>
    <% end %>
  </ol>
</div>

<%= form_for ([@list, @guest])do |f|  %>
  <%= f.text_field :name %>
  <p><%= f.submit "Add guest" %></p>
<% end %>
